#!/bin/bash
# Alaterm:file=$alatermTop/usr/bin/$launchCommand
# If you are in Alaterm and issue its launch command, instead:
echo -e "\e[33mYou cannot launch Alaterm from within Alaterm.\e[0m"
# If you were looking for the backup copy of the actual launch command,
# Look in Alaterm /usr/local/scripts.
##
